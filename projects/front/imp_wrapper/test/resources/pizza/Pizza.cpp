#include "Pizza.h"

std::string Pizza::add_topping(int value) {
  size = value;
  auto y = "Hello World";

  if (true) {
    return y;
  }

  auto i = 0;
  while (i < 10) {
    i += 1;
  }

  do {
    i -= 2;
  }
  while (i > 0);

  return "";
}