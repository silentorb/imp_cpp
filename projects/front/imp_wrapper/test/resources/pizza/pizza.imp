
Pizza {

    size

    add_topping(value) {
        size = value
        var y = "Hello World"
        if (true)
            return y

        var i = 0
        while (i < 10) {
         i += 1
        }

        do {
         i -= 2
        }
        while (i > 0)

        return ""
    }

    crazy() {
      var k = add_topping(12)
    }
}